import {LoginForm} from "@/components/auth/login-form"

export default function Login(){
    return (
        <div className="md:max-w-xl items-center justify-center mx-auto !border-none">
        <LoginForm/>
        </div>
);
}